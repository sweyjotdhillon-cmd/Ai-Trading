# Custom User Instructions

- **Timeframe Rule:** If the user provides a three-minute time graph, ensure the fetch behavior matches (e.g., fetch data for a three-candle / five-time graph configuration as directed). 
- **Continuous Live Analysis:** If live analysis is active, the data fetching and analysis must execute continuously.
- **Decision Rationale:** The decision of giving a point or not giving a point MUST NOT be random. It should always have a proper, verifiable reason backing the evaluation technique.
- **Imagination Constraint:** Continuously check that no variables or code features are generating mock data or "imagining" values without reason. No mock data should be used.
